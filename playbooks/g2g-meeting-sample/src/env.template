# Service app (or test) access token for Webex APIs — never commit real values.
WEBEX_ACCESS_TOKEN=

# Local helper HTTP port (runChromeTab.js). Default 3000 if unset.
# PORT=3000

# macOS only: application name for `open -a` (default Google Chrome).
# CHROME_APP_NAME=Google Chrome
