# Copy to .env in this directory (same folder as app.py). Do not commit .env.

# Bot access token from https://developer.webex.com (Messaging scopes)
WEBEX_BOT_TOKEN=

# Target Webex space (room) ID — bot must be a member of the space
WEBEX_ROOM_ID=

# Optional: HTTP listen port (default 5000)
# PORT=5000
