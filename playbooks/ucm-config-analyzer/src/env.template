# Copy to .env and adjust values. Used by type_user_association.py when run via main().
# main.py and simple.py do not require environment variables.

# Absolute or relative path to a UCM bulk-export .tar file.
# Example: /path/to/ucm_export.tar
TAR_FILE=
