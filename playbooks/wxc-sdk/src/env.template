# Webex-side variables for running upstream wxc_sdk examples locally.
# Copy to .env in your project or examples/ directory (see upstream examples/.env (sample)).
# Never commit real tokens.

# Personal or bot access token with scopes required for the APIs you call.
WEBEX_ACCESS_TOKEN=
