# Copy to .env in this directory (same folder as package.json) if you use CRA env vars.
# cp env.template .env

# -----------------------------------------------------------------------------
# Local dev server (optional — Create React App / react-scripts)
# -----------------------------------------------------------------------------
# PORT=3000
# BROWSER=none

# -----------------------------------------------------------------------------
# Extend this sample — real CRM / line-of-business API (optional)
# -----------------------------------------------------------------------------
# REACT_APP_CRM_API_BASE_URL=https://api.example.com/v1
# REACT_APP_CRM_API_KEY=

# This sample does not read these variables yet; add fetches in App.js (or a
# service module) when you replace mock data from generateCustomer.js.
